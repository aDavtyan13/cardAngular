import { DamDirective } from './dam.directive';

describe('DamDirective', () => {
  it('should create an instance', () => {
    const directive = new DamDirective();
    expect(directive).toBeTruthy();
  });
});
